import Link from 'next/link'
import { FileQuestion, ShieldCheck } from 'lucide-react'

export function AuthPage({ title, eyebrow, description, children }: { title: string; eyebrow: string; description: string; children: React.ReactNode }) {
  return <main className="flex min-h-svh flex-col bg-background lg:flex-row"><section className="hidden flex-1 flex-col justify-between bg-primary p-10 text-primary-foreground lg:flex"><Link href="/login" className="font-serif text-3xl font-semibold tracking-tight">Qtor<span className="text-brand">.</span></Link><div className="max-w-md"><FileQuestion className="mb-6 text-brand" size={36} /><h2 className="font-serif text-5xl leading-tight">Thoughtful papers, built with less busywork.</h2><p className="mt-5 text-base leading-7 text-primary-foreground/75">A calm workspace for educators to turn great questions into professional assessments.</p></div><p className="text-sm text-primary-foreground/60">Secure email and password authentication</p></section><section className="flex flex-1 flex-col px-5 py-8 sm:px-10 lg:max-w-xl lg:justify-center lg:px-20"><Link href="/login" className="mb-14 font-serif text-3xl font-semibold tracking-tight lg:hidden">Qtor<span className="text-brand">.</span></Link><div className="mx-auto w-full max-w-md"><div className="mb-8"><p className="mb-3 text-sm font-semibold uppercase tracking-[0.16em] text-primary">{eyebrow}</p><h1 className="font-serif text-4xl leading-tight tracking-tight text-balance">{title}</h1><p className="mt-3 leading-6 text-muted-foreground">{description}</p></div>{children}<div className="mt-10 flex items-center justify-center gap-2 text-xs text-muted-foreground"><ShieldCheck size={14} /> Your session is protected by Qtor</div></div></section></main>
}
